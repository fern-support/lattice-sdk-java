/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.anduril.resources.tasks.types;

import com.anduril.types.StreamHeartbeat;
import com.anduril.types.TaskStreamEvent;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StreamTasksResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StreamTasksResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StreamTasksResponse heartbeat(StreamHeartbeat value) {
        return new StreamTasksResponse(new HeartbeatValue(value));
    }

    public static StreamTasksResponse taskEvent(TaskStreamEvent value) {
        return new StreamTasksResponse(new TaskEventValue(value));
    }

    public boolean isHeartbeat() {
        return value instanceof HeartbeatValue;
    }

    public boolean isTaskEvent() {
        return value instanceof TaskEventValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<StreamHeartbeat> getHeartbeat() {
        if (isHeartbeat()) {
            return Optional.of(((HeartbeatValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TaskStreamEvent> getTaskEvent() {
        if (isTaskEvent()) {
            return Optional.of(((TaskEventValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitHeartbeat(StreamHeartbeat heartbeat);

        T visitTaskEvent(TaskStreamEvent taskEvent);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "event", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(HeartbeatValue.class), @JsonSubTypes.Type(TaskEventValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("heartbeat")
    @JsonIgnoreProperties("event")
    private static final class HeartbeatValue implements Value {
        @JsonUnwrapped
        private StreamHeartbeat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private HeartbeatValue() {}

        private HeartbeatValue(StreamHeartbeat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitHeartbeat(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof HeartbeatValue && equalTo((HeartbeatValue) other);
        }

        private boolean equalTo(HeartbeatValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamTasksResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("task_event")
    @JsonIgnoreProperties("event")
    private static final class TaskEventValue implements Value {
        @JsonUnwrapped
        private TaskStreamEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TaskEventValue() {}

        private TaskEventValue(TaskStreamEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTaskEvent(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TaskEventValue && equalTo((TaskEventValue) other);
        }

        private boolean equalTo(TaskEventValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamTasksResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("event")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamTasksResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
